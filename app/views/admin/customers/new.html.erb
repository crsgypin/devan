<%= form_for setup_customer(@customer), :url=>customers_path do |form| %>

	<div class="heading-box">
		<div class="heading-inner">
			<div class="info-box">
				<h2 class="title">
				  新增客戶
				</h2>

				<div class="message">
					<% if flash[:success].present? %>
						<div class="success"> <%= flash[:success] %> </div>
					<% elsif flash[:fail].present? %>
						<div class="fail"> <%= flash[:fail] %> </div>
					<% end %>
				</div>

				<ul class="sub-nav">
					<li> <%= link_to "回客戶清單", customers_path %> </li>
				</ul>			
			</div>

			<div class="submit-box"> 
				<%= form.submit "繳交表單", :class=>"btn btn-primary"	%>
			</div>
		</div>
	</div>

	<div class="content-box">
		<div class="content-inner">
		
		<%= render :partial=>'customers/form', :locals=>{:form=>form} %>

		</div> <%# content-inner %>
	</div> <%# content-box %>
<% end %>

